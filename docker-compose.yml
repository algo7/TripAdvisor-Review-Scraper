version: '3.9'
services:
  # Service name
  scraper: 
    # Build directory
    build: .
    # Image name
    image: ghcr.io/tripadvisor-review-scraper/scrap:latest
    # Env file
    env_file: env
    volumes:
      - type: bind
        source: ./reviews
        target: /puppeteer/data
