version: '3.9'
services:
  # Service name
  scraper: 
    # Build directory
    build: .
    # Image name
    image: scrap:latest
    # Env file
    env_file: env
    volumes:
      - type: bind
        source: ./reviews
        target: /puppeteer/data
